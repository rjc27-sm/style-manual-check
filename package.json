{
  "name": "style-manual-check",
  "version": "2.0.0",
  "description": "Checks Word documents and text against the Australian Government Style Manual. Runs entirely in the browser.",
  "type": "module",
  "scripts": {
    "test": "node tests/run-tests.mjs tests/sample.docx && node tests/tracked-changes.mjs && node tests/retrieval.mjs && node tests/list-convention.mjs && node tests/list-format-client.mjs"
  },
  "devDependencies": {
    "@xmldom/xmldom": "^0.9.0",
    "jszip": "^3.10.1",
    "docx": "^9.7.1"
  }
}
